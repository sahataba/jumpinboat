# Browser + SSR fetch base for public boat data (often the Next app origin in prod)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Effect API origin: Next rewrites /api/* here (default http://localhost:4000). On Vercel, set to your deployed API URL.
# API_BASE_URL=https://your-api.example.com
